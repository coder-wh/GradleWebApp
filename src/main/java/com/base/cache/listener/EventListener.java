package com.base.cache.listener;

/*import org.ehcache.event.CacheEvent;
import org.ehcache.event.CacheEventListener;

import com.base.cache.album.AlbumCache;

public class EventListener implements CacheEventListener<String, AlbumCache> {

	@Override
	public void onEvent(CacheEvent<? extends String, ? extends AlbumCache> event) {
		// TODO Auto-generated method stub
		System.out.println(event.getKey() + ", " + event.getType() + ", timestamp in milsecond: " + System.currentTimeMillis());
	}

	
}
*/